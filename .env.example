NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
NEXT_PUBLIC_APP_URL=

# Require an approved action plan when issuing a new FRA. Enable after the
# FRA action schema, storage bucket, and publication workflow are deployed.
FRA_ACTION_PLAN_REQUIRED=false

# Admin-only Audit Studio. Set false to disable for this deployment.
NEXT_PUBLIC_AUDIT_STUDIO_ENABLED=true
